{% extends 'base.html.twig' %}

{% block title %}Edit Slug{% endblock %}

{% block body %}
    <h1>Edit Slug</h1>

    {{ include('slug/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('slug_index') }}">back to list</a>

    {{ include('slug/_delete_form.html.twig') }}
{% endblock %}
